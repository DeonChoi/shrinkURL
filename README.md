# shrinkURL

shrinkURL is a full stack web application that was built using the MERN stack. It allows registered/logged-in users to shorten long URLS. The resulting short URLs are stored specific to their user accounts and can only be accessed by them.
 
